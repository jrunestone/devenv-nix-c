# devenv-nix-c
NixOS developer environment for C89/C90 with CMake/Ninja/ccache

## Compile and run
1. `nix develop`
2. Replace `projectname` with your project name
3. Run cmake config task to output language server files
4. Check tasks.json for respective IDE
